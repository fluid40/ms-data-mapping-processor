services:
  opcua_press_mockup:
    image: engineeringmethodsag/fluid-opcua-press-mockup:latest
    container_name: opcua-press-mockup
    networks:
      - bridge

  asset_connector:
    image: engineeringmethodsag/ms-asset-connector:latest
    container_name: ms-asset-connector
    depends_on:
      - opcua_press_mockup
    networks:
      - bridge

  aas-registry:
    image: eclipsebasyx/aas-registry-log-mongodb:2.0.0-SNAPSHOT
    container_name: aas-registry
    environment:
      - SERVER_PORT=8080
    volumes:
      - ./basyx/aas-registry.yml:/workspace/config/application.yml
    restart: always
    depends_on:
      mongo:
        condition: service_healthy
    ports:
      - "8076:8080"
    networks:
      - bridge

  sm-registry:
    image: eclipsebasyx/submodel-registry-log-mongodb:2.0.0-SNAPSHOT
    container_name: sm-registry
    environment:
      - SERVER_PORT=8080
    volumes:
      - ./basyx/sm-registry.yml:/workspace/config/application.yml
    restart: always
    depends_on:
      mongo:
        condition: service_healthy
    ports:
      - "8077:8080"
    networks:
      - bridge

  java_aas_server:
    image: eclipsebasyx/aas-environment:2.0.0-SNAPSHOT
    container_name: aas-server
    ports:
      - "8075:8075"
    environment:
      - SERVER_PORT=8075
      - SERVER_HOST=javaaasserver
    volumes:
      - ./aas:/application/aas
      - ./basyx/aas-env.properties:/application/application.properties
    depends_on:
      aas-registry:
        condition: service_healthy
      sm-registry:
        condition: service_healthy
      mongo:
        condition: service_healthy
    networks:
      - bridge

  aas-web-ui:
    image: eclipsebasyx/aas-gui:SNAPSHOT
    container_name: aas-web-ui
    ports:
      - "8078:3000"
    environment:
      AAS_REGISTRY_PATH: http://localhost:8076/shell-descriptors
      SUBMODEL_REGISTRY_PATH: http://localhost:8077/submodel-descriptors
      AAS_REPO_PATH: http://localhost:8075/shells
      SUBMODEL_REPO_PATH: http://localhost:8075/submodels
      CD_REPO_PATH: http://localhost:8075/concept-descriptions
      PRIMARY_COLOR: "#00A0C7"
      LOGO_LIGHT_PATH: fluid40_light.png
      LOGO_DARK_PATH: fluid40_dark.png
      DASHBOARD_SERVICE_PATH:
    volumes:
      - ./logo:/usr/src/app/dist/Logo
    depends_on:
      java_aas_server:
        condition: service_healthy
    networks:
      - bridge

  mongo:
    image: mongo:4.4
    container_name: mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: mongoAdmin
      MONGO_INITDB_ROOT_PASSWORD: mongoPassword
    volumes:
      - mongo_data:/data/db
    restart: always
    healthcheck:
      test: mongo
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - bridge

  dev_container:
    build: .
    container_name: ms-dev-container
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
    depends_on:
      java_aas_server:
        condition: service_healthy
      asset_connector:
        condition: service_started
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - IQSTRUCT_AAS_SERVER_SECRET=nY0mjyECF60DGzNmQUjL81XurSl8etom
      - HACK_AAS_SERVER_SECRET=fl40-hack4
      - AAS_REGISTRY_SECRET=fl40-hack4
      - RUNTIME_CONFIGURATION_FILE=./configuration/DevContainerEnv.json
    networks:
      - bridge

networks:
  bridge:


volumes:
  mongo_data:
